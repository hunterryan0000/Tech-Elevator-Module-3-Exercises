package com.techelevator.farm;

public class LittleDog extends Dog {

    // Because I am not defining a constructor here
    // Java (behind the scenes) adds a no arg constructor that calls super() with no args.
    // This works, because Dog has a no arg constructor.
}
